module acme.testing.self_correction

service selfCorrectingService
  goal "Test self correction capabilities of the generator."

  operation Process
    inputs:
      input_val string
    outputs:
      result string
    tests:
      - scenario: "Process successfully"
        expect: result == "ok"

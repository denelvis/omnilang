module acme.greet

/// A simple greeting service
service GreetService {
  goal: "Say hello to users"

  /// Greets a user by name
  rpc Greet(name: String) -> String {
    preconditions:
      - name != ""
  }
}
